{
    "dependencies": [
        "lightdm",
        "light-locker"
    ],
    "install": [
        "cd .. && mkdir -p tmp && cd tmp && git clone https://github.com/josephsurin/lightdm-mini-greeter.git && cd lightdm-mini-greeter && ./autogen.sh && ./configure --datadir /usr/share --bindir /usr/bin --sysconfdir /etc && make && sudo make install",
        "copy lightdm-mini-greeter.conf /etc/lightdm/lightdm-mini-greeter.conf",
        {
            "question": "Please enter your username.",
            "variable": "{username}",
            "answer": {
                "*": [
                    "replace /etc/lightdm/lightdm-mini-greeter.conf [USERNAME] {username}"
                ]
            }
        },
        "copy lightdm.conf /etc/lightdm/lightdm.conf",
        "sudo systemctl enable lightdm"
    ]
}